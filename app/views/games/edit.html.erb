<h2>Edit Game</h2>

<%= form_for @game do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :group_id %>
  <%= f.text_field :group_id %>

  <%= f.submit %>
<% end %>

<%= form_for @game, method: :delete do |f| %>
  <%= f.submit "Delete!", class: "delete" %>
<% end %>
